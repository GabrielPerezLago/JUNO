
services: 
  postgres:
    image: postgres:15
    environment: 
      POSTGRES_USER: ${POSTGRES_DB_USER}
      POSTGRES_PASSWORD: ${POSTGRES_DB_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB_NAME}
    volumes:
      - ./docker/postgres/db_v3.0.sql:/docker-entrypoint-initdb.d/db_v3.0.sql
      - postgres_data:/var/lib/postgresql/data
    ports: 
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "sh -c 'pg_isready -U ${POSTGRES_DB_USER} -d ${POSTGRES_DB_NAME}'"]
      interval: 30s
      timeout: 10s
      retries: 3

      

  juno:
    build:
      context: ./juno_backend
      dockerfile: Dockerfile
    container_name: juno_serv
    environment:
      - DB_URI=jdbc:postgresql://postgres:5432/${POSTGRES_DB_NAME}
      - POSTGRES_DB_USER=${POSTGRES_DB_USER}
      - POSTGRES_DB_PASSWORD=${POSTGRES_DB_PASSWORD}
      - JWT_SECRET_KEY=${JWT_SECRET_KEY}
      - JWT_TOKEN_EXPIRATION=${JWT_TOKEN_EXPIRATION}
      - JWT_REFRESH_TOKEN_EXPIRATION=${JWT_REFRESH_TOKEN_EXPIRATION}
      - JWT_TOKEN_ISSUER=${JWT_TOKEN_ISSUER}
    ports:
      - "8080:8080"
    depends_on:
      postgres:
        condition: service_healthy
volumes:
  postgres_data: